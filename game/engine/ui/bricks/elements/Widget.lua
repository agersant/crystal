require("engine/utils/OOP");
local Wrapper = require("engine/ui/bricks/core/Element");

local Widget = Class("Widget", Wrapper);

Widget.init = function(self)
	Widget.super.init(self);
end

-- TODO

return Widget;
